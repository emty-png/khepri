[package]
name = "khepri-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
khepri-core = { path = "../khepri-core" }
serde = { version = "1", features = ["derive"] }
ron = "0.8"

[dev-dependencies]
tempfile = "3"
